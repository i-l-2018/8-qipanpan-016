当把在本地变动的内容同步到远程仓库时：
  所编辑的内容会存在于工作区，工作区中包含了仓库的工作文件，可以修改内容--->将工作区的内容移到暂存区（在我看来，暂存区是工作区用来提交更改（commit）前可以
  暂存工作区的变化的区域）--->再将暂存区的内容提交（commit)到本地仓库（即本地硬盘类似的保存）--->再将本地仓库中保存的更改内容Push到远程仓库
当把远程仓库变动同步到本地时：
  将远程仓库中的内容抓取（Fetch from) 到本地仓库一份--->再从本地仓库经过相融（merge)到工作区(在我看来，工作区就是一个可以进行编辑，更改内容操作，且可以显
  示内容的区域）
